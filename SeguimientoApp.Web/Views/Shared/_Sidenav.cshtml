@{
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();
    var action = ViewContext.RouteData.Values["action"]?.ToString();
}

<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-dark" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">
                <!-- Personas -->
                <div class="sb-sidenav-menu-heading">Personas</div>
                <a class="nav-link @(controller == "Personas" && action == "Index" ? "active" : "")" asp-controller="Personas" asp-action="Index">
                    <div class="sb-nav-link-icon"><i class="fas fa-users"></i></div>
                    Votantes
                </a>
                <a class="nav-link @(controller == "Personas" && action == "ImportarVotantes" ? "active" : "")" asp-controller="Personas" asp-action="ImportarVotantes">
                    <div class="sb-nav-link-icon"><i class="fas fa-file-csv"></i></div>
                    Importar votantes
                </a>

                <!-- Eventos -->
                <div class="sb-sidenav-menu-heading">Eventos</div>

                @* <a class="nav-link @(controller == "Eventos" ? "active" : "")" asp-controller="Eventos" asp-action="Index">
                    <div class="sb-nav-link-icon"><i class="fas fa-calendar-alt"></i></div>
                    Eventos
                </a> *@

                <!-- Seguimiento -->
                <div class="sb-sidenav-menu-heading">Seguimiento</div>
                @* <a class="nav-link collapsed @(controller == "Seguimientos" ? "active" : "")" href="#" data-bs-toggle="collapse" data-bs-target="#collapseSeguimiento" aria-expanded="false" aria-controls="collapseSeguimiento">
                    <div class="sb-nav-link-icon"><i class="fas fa-columns"></i></div>
                    Seguimiento
                    <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                </a>
                <div class="collapse" id="collapseSeguimiento" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                    <nav class="sb-sidenav-menu-nested nav">
                        <a class="nav-link @(controller == "Seguimientos" ? "active" : "")" asp-controller="Personas" asp-action="Index">Eventos</a>
                    </nav>
                </div> *@
            </div>
        </div>
        <div class="sb-sidenav-footer">
            <div class="small">Conectado como:</div>
            Start Bootstrap
        </div>
    </nav>
</div>
